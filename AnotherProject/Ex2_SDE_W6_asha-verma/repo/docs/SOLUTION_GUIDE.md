# ProjectFlow Week 6 — Solution Guide

The implementation follows Route -> Schema -> Service -> Repository -> ORM, with pure domain objects kept separate from framework/database concerns.

Key concepts demonstrated:
- specification-driven development
- domain modelling and aggregate boundaries
- REST/nested resources
- Pydantic v2 validation
- FastAPI dependency injection
- authentication vs authorization teaching model
- RBAC
- SQLAlchemy 2.x ORM and Session transactions
- repository and service patterns
- state-machine validation
- relational constraints and joins
- audit logs
- aggregate summary queries
- Alembic migrations
- Docker and CI
- unit/API testing with dependency overrides

See the separately delivered Word solution explanation for a full topic-by-topic walkthrough.
