# Legacy refactor module
The original lab helper used deeply nested `if/else` conditions to classify a task. The refactored function uses guard clauses while preserving the same externally visible behaviour. Tests document the behaviour that must not change.
